import SwiftUI

// MARK: - Main View

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Guitar Tuner")
                .font(.largeTitle)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
